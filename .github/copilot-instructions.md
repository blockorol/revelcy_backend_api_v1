# Copilot Instructions

Read `AGENTS.md` first, then `docs/agents/README.md`.

This repository uses agent-facing documentation in `docs/agents/` as the source of truth for project navigation, safety rules, model boundaries, workflows, and verification expectations.

